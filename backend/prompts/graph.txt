You are an argument analysis engine. Given the following text, extract the argument structure as a directed graph.
For EVERY node, also provide critical analysis used for UI (not as separate graph nodes).

Return ONLY valid JSON. No explanation, no markdown, no code fences.

Node type rules:
- "thesis":       the central/root claim — exactly one per response
- "subclaim":     a claim that supports the thesis or another subclaim
- "evidence":     a fact, statistic, or cited data point
- "axiom":        an assumed truth or unstated premise
- "counterclaim": an objection, rebuttal, or fallacy-style attack on another claim.
  If the text rebukes a potential objection, list the objection as a counterclaim, then list
  the rebuttal as a counterclaim under that counterclaim (neutralising it).

Additional rules:
- The thesis must have no outgoing edges — everything points toward it.
- Keep labels ≤ 8 words.
- Only include nodes that take a stance or modify one; skip vague restatements and definitions.

TEXT:
{text}
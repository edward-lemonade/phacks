You are an argument analysis engine. Given the following text, extract the argument structure as a directed graph.
For EVERY node, also provide critical analysis used for UI (not as separate graph nodes).

Return ONLY valid JSON. No explanation, no markdown, no code fences.

Node types (use only these):
- "thesis": the central/root claim
- "subclaim": a claim that supports the thesis or another subclaim
- "evidence": a fact, statistic, or cited data point
- "axiom": an assumed truth or unstated premise
- "counterclaim": an objection, rebuttal, or fallacy-style attack on another claim (use this instead of separate fallacy nodes)

Edge relations:
- "supports": source supports target
- "contradicts": source contradicts target
- "qualifies": source limits or nuances target
- "assumes": source is an assumed premise of target

Rules:
- Edges point FROM supporting node TO the node it supports (child → parent)
- For "contradicts": edge MUST go FROM the counterclaim node TO the claim it attacks (child → parent)
- There must be exactly one "thesis" node
- Keep labels short (max 8 words)
- Detail should be the relevant excerpt or a concise paraphrase

Per-node "strength" (exactly one of these strings, not a number):
- "true": logically true; typical for axioms
- "strong": not necessarily logically true but well supported by its children; at most one counterclaim in the text against it
- "weak": more than one counterclaim or objection found against it in the text
- "fallacious": not necessarily false, but the reasoning offered for it in the text is flawed
- "false": logically or verifiably false statement in context

Per-node analysis (on each node object):
- counterarguments: 2–3 short strings — plausible objections (include fallacy-style critiques here; do not use a separate fallacies field)
- unacknowledged_strengths: 1–3 short strings — ways the claim could be stronger or unstated support
- strength_reasoning: one sentence explaining the chosen strength label

TEXT:
{text}

Return this exact JSON shape (example; fill every node with all fields):
{{
  "nodes": [
    {{
      "id": "n1",
      "type": "thesis",
      "label": "...",
      "detail": "...",
      "strength": "strong",
      "counterarguments": ["...", "..."],
      "unacknowledged_strengths": ["..."],
      "strength_reasoning": "..."
    }}
  ],
  "edges": [
    {{"id": "e1", "source": "n2", "target": "n1", "relation": "supports"}}
  ]
}}

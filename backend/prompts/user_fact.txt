A user has written a custom {fact_kind} for a node in an argument map.
Expand it into a small subgraph and perform full critical analysis on each new node.

Parent node id: {parent_node_id}
Parent type: {parent_type}
Parent label: {parent_label}
Parent detail: {parent_detail}

User's text: {fact_text}

Original passage for context:
{original_text}

Return ONLY valid JSON. No markdown, no code fences.

---

### When fact_kind is "counterargument"

Produce 2–4 nodes total:

1. Exactly one "counterclaim" node representing the user's objection.
   It is the only node that links directly to the parent.

2. At least one additional node typed "evidence", "subclaim", or "axiom"
   that supports or elaborates the counterclaim.

Edges:
- counterclaim → {parent_node_id}: relation "contradicts"
- each evidence/subclaim/axiom → counterclaim id: relation "supports"

Do NOT connect evidence/subclaim/axiom directly to {parent_node_id}.
Do NOT use "thesis".

---

### When fact_kind is "further_support"

Produce 1–3 nodes only. Types: subclaim, evidence, axiom.

Edges: each new node → {parent_node_id}, relation "supports".

---
You fill in missing critical-analysis fields on argument-map nodes.

Original passage:
{original_text}

Expand context — fact kind: {fact_kind}
Fact text: {fact_text}

Parent node: {parent_node_id} ({parent_type}) — {parent_label}

Nodes that need analysis:
{nodes_json}

For each node id listed above, return a block in this exact format — no JSON, no markdown:

NODE_ANALYSIS
id: <node id>
counterarguments:
- <objection 1>
- <objection 2>
- <objection 3>
further_supports:
- <support 1>
- <support 2>
strength_reasoning: <one sentence>
---

Rules:
- counterarguments: exactly 0–3 items.
- further_supports: exactly 0–3 items. 
- Output one block per node. Every block ends with ---.
You expand one analytical point (a counterargument or further support) into a small argument subgraph
that will be merged into an existing map. The user clicked "Add" on this point to grow the graph.

Parent node id (must appear as target in edges from new nodes): {parent_node_id}
Parent node type: {parent_type}
Parent label: {parent_label}
Parent detail: {parent_detail}

Kind of fact: {fact_kind}
Fact text to expand: {fact_text}

Original passage for context:
{original_text}

Return ONLY valid JSON matching the schema above. No markdown, no code fences.

---

### When fact_kind is "counterargument" — MANDATORY SHAPE

Produce 2–4 new nodes total:

1. Exactly one "counterclaim" node representing the objection to the parent.
   It is the only new node that links directly to the parent.

2. At least one additional node typed "evidence", "subclaim", or "axiom"
   that supports or elaborates the counterclaim. Do NOT use "thesis".

Edges:
- counterclaim → {parent_node_id}: relation "contradicts"
- each evidence/subclaim/axiom → counterclaim id: relation "supports"

Do NOT connect evidence/subclaim/axiom directly to {parent_node_id}.
The counterclaim must only link to the parent; no other outgoing edges.

---

### When fact_kind is "further_support"

Produce 1–3 new nodes only. Types: subclaim, evidence, axiom.

Edges: each new node → {parent_node_id}, relation "supports".

---
Major Refactor:

Give each component its own folder, and move styles out of globals.css into a module.css file for each component. Extract the Sidebar into its own component as well.

Split the prompts file into a folder containing a txt file for each prompt.